# План ребрендинга и подготовки к самостоятельному релизу

## 1. Подготовка репозитория
- [x] Создать новую ветку для ребрендинга (`rebranding_preparation`)
- [ ] Определить стратегию работы с ветками (в будущем `rebranding_preparation` -> `main` или `master`)

## 2. Аудит авторских прав (Copyright Audit)
Цель: Восстановить и актуализировать информацию о копирайте во всех файлах.
- [x] **Config*.qml файлы**:
    - [x] Найти исходные копирайты для перемещенных/модифицированных файлов конфигурации.
    - [x] Восстановить заголовки лицензии GPL.
    - [x] Добавить `Copyright (C) 2024-2026 ...` с вашим именем, если вы вносили существенные изменения.
- [x] **Остальные .qml и .js файлы**:
    - [x] Проверить наличие заголовков лицензии во всех файлах исходного кода.
    - [x] Добавить ваше авторство в файлы, которые вы значительно изменяли или создавали с нуля.
- [ ] **Чистка кода (Code Cleanup)**:
    - [x] Найти и удалить все комментарии на русском языке.
    - [x] Удалить ненужные/устаревшие комментарии (оставленные в процессе разработки).
- [ ] **Скрипты и прочее**:
    - [x] Проверить `.sh` скрипты в корне (`build.sh`, `install.sh`, `update.sh`, `testing.sh`, `iconinstall.sh`).
    - [x] Проверить скрипты в `package/translate/` (`build`, `merge`, `template`, `clear`).
    - [x] Проверить `package/metadata.json` (будет обновлен на этапе 3).
    - [x] Обновить `README.MD`.

## 3. Техническая отвязка (Identity Shift)
Цель: Сделать плазмоид уникальным для системы KDE Plasma.
- [x] **metadata.json**:
    - [x] Изменить `Id` (например, на `io.github.daydve.fancytasksng` или подобное).
    - [x] Изменить `Name` (на `Fancy Tasks NG` или выбранное имя).
    - [x] Обновить список `Authors` (добавить себя как текущего мейнтейнера/разработчика).
    - [x] Обновить ссылки (`Website`, `BugTrackerUrl`).
- [x] **Код QML**:
    - [x] Проверить, не используются ли жестко прописанные ID или пути, завязанные на старое имя.

## 4. Обновление документации и метаданных
- [x] **README.MD**:
    - [x] Полный рерайт: убрать бейджи старых CI, актуализировать описание.
    - [x] Четко обозначить статус проекта: "Форк/Реборн для Plasma 6.5+".
    - [x] Обновить секцию `Credits` (упомянуть авторов оригинала).
    - [x] Обновить инструкции по установке (убрать устаревшие скрипты, добавить актуальные методы).
- [ ] **Очистка**:
    - [ ] Удалить устаревшие скрипты (`install.sh` под Plasma 5, если они не работают).
    - [ ] Обновить `.gitignore` и прочие служебные файлы.

## 5. Финальная проверка и релиз
- [x] Тестирование чистой установки (убедиться, что новый ID не конфликтует со старым, если он установлен).
- [x] Создание тега релиза (например, `v0.1.0-ng` или `v3.0.0`).
